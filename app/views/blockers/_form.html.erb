<%= form_for(@blocker) do |f| %>
  <% if @blocker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blocker.errors.count, "error") %> prohibited this blocker from being saved:</h2>

      <ul>
      <% @blocker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :blockers_id %><br />
    <%= f.text_field :blockers_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
